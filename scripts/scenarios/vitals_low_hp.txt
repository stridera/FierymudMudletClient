# Low-HP state — vitals gauge should flash red, color band <= 0.15
# Run: scripts/mock_mud.py --scenario scripts/scenarios/vitals_low_hp.txt
gmcp Char.Status {"name":"TestUser","class":"Sorcerer","level":105,"xp":12345,"wealth":50000}
gmcp Char.Vitals {"hp":20,"max_hp":200,"mv":50,"max_mv":100,"next_level_pct":35,"string":"H:20/200 M:50/100"}
delay 0.3
prompt
delay 1.5
# State assertions — verify Mudlet actually applied the frames.
# Each assert runs via MREXEC; truthy result passes.
assert gmcp.Char.Status.name == "TestUser"
assert gmcp.Char.Vitals.hp == 20
assert gmcp.Char.Vitals.max_hp == 200
assert FierymudRs.Character.Vitals.hp == 20
screenshot vitals_low_hp
